/** Google OAuth dönüşünde LoginSuccessPage'in hedef rotayı seçmesi için sessionStorage anahtarı. */
export const SESSION_POST_OAUTH_REDIRECT_KEY = "nb_post_oauth_redirect";

/** Admin paneline yönlendirme isteği (`/admin-login` akışı). */
export const SESSION_POST_OAUTH_ADMIN_VALUE = "admin";
