[pytest]
# asyncio_mode=auto: @pytest.mark.asyncio gerektirmeden async testler otomatik çalışır.
asyncio_mode = auto
markers =
    asyncio: async test (pytest-asyncio tarafından işlenir)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
