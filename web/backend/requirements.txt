fastapi
uvicorn
gunicorn  # üretim WSGI/ASGI sunucusu (gunicorn.conf.py + UvicornWorker ile başlatılır)
sqlalchemy>=2.0
slowapi>=0.1.9
httpx>=0.27.0
python-multipart
PyJWT>=2.8.0
bcrypt>=4.1.0
pydantic-settings>=2.0.0
email-validator>=2.0.0

# PDF motoru (proje kökündeki src.pdf_engine): /api/pdf-to-word, sıkıştırma vb.
pikepdf>=10
pymupdf>=1.24
pdf2docx>=0.5
pytesseract
ocrmypdf
# NOT: docling / easyocr / onnxruntime (torch + CUDA, ~5 GB) buradan ÇIKARILDI.
# Ücretsiz/küçük planlara sığmaz. Yapay zeka tabanlı OCR gerektiğinde (büyük
# planda) requirements-ocr.txt dosyasını da kurun: pip install -r requirements-ocr.txt
pypdf>=6
python-docx
Pillow
lxml
pdf2image
openpyxl
reportlab
pdfplumber
python-pptx
xhtml2pdf
# NOT: pdfkit kaldırıldı (CVE-2025-26240). HTML→PDF için wkhtmltopdf ikilisi
# artık doğrudan subprocess ile çağrılıyor (bkz. src/pdf_toolkit_extra.py).
img2pdf

# S3-uyumlu nesne depolama (AWS S3 / Cloudflare R2)
boto3>=1.34.0

# Hata izleme
sentry-sdk[fastapi]>=2.0.0

# Billing / invoicing
tenacity>=8.2.0
sendgrid>=6.10.0
# app/billing/{birfatura,parasut,tcmb,email_service}.py doğrudan requests kullanır ve
# app/main.py bunları internal_billing router'ı üzerinden AÇILIŞTA import eder. Şimdiye
# kadar başka bir paketin transitif bağımlılığı olarak geldiği için fark edilmemişti;
# beyan edilmediğinden CI'da çözülmüyordu (ModuleNotFoundError: requests).
# >=2.32.0: CVE-2024-35195 (Session doğrulama atlatma) düzeltmesi.
requests>=2.32.0

# Test
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Güvenlik: ortamdaki setuptools 79.0.1 -> PYSEC-2026-3447 (sdist Unicode bypass).
# Fix >=83.0.0; pip install sırasında yükseltilir (pip-audit temiz geçer).
setuptools>=83.0.0
